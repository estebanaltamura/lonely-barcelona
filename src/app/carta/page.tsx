'use client';

import { Box } from '@mui/material';
import React from 'react';

const Carta: React.FC = () => {
  return (
    <Box sx={{ width: '100vw', height: '100%' }}>
      <img src='/carta/carta-web-final.jpg' alt='' style={{ width: '100%' }} />
    </Box>
  );
};

export default Carta;
